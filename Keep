function keepFindingImage("start.png", 1, 1, nil, 1000, exit)
    local f = function()
        findImage("start.png", 1, 1, nil, nil);
        for a, b in pairs(f) do
        if a = true then
        exit = true;
        else keep;
        end
    end

    keep(f, 1000, exit);
    
end
