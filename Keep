function imagecheck(s)
    local r = findImage ("start.png", 1, 0.9, nil, nil);
    n = 0;
    for i, v in pairs(r) do
        n = n + 1;
    end
    return n;
end

if imagecheck(start.png) == 0 then
  tap(45,450)
end
