local result = findImage("start.png", 1, 1, nil, nil};
for i, v in pairs(result) do
    tap(45, 450);
end

local f1 = function(locations)
    for i, v in pairs(locations) do
        tap (138, 555);
    end
end

local exit = false;
keepFindingImage("start.png", 1, 1, nil, nil, f1, 1000, exit);

break end
